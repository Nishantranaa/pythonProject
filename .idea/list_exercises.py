def numbers_to_list(): # this function will ask the user to input 5 numbers and will then add them to a variable called
# numbers.

numbers = []
count = 5
for i in count(1, 5 +1):

    users_input = print(input("Number: "))
    numbers.append(users_input)